import vagones.*
import formaciones.*

describe "verificamos una formación"{
	const vp1 = new VagonDePasajeros(largo=10,ancho=2,estaOrdenado=true)
	const vp2 = new VagonDePasajeros(largo=10,ancho=2,estaOrdenado=false)
	const vc1 = new VagonDeCarga(cargaMaximaIdeal=)
	const vd1 = new VagonDormitorio(largo=10,ancho=4,estaOrdenado=false)

	test ""{
		
	}
	
	
	
	
}